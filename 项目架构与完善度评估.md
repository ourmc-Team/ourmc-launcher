# OML 启动器 — 项目架构与完善度评估

> 面向团队的技术文档。内容基于对全量源码的逐文件审查，反映**当前实际状态**（README 部分内容已滞后于代码，本文以代码为准）。
> 评估时间：2026-07。维护负责人：内核由本文档作者负责，正版微软登录由他人负责。

---

## 一、技术栈与框架

| 项 | 选型 | 说明 |
|---|---|---|
| 运行时 | .NET 8.0 (`net8.0-windows`) | 单一 Windows 桌面目标 |
| UI 框架 | Blazor WebView (`Microsoft.AspNetCore.Components.WebView.WindowsForms` 8.0.*) | 用 WebView2 渲染 Razor 组件 |
| 宿主容器 | Windows Forms | `LauncherForm` 自定义无边框窗口（圆角/深色/拖拽/淡入动画，调 dwmapi/user32） |
| 语言 | C# 12 / Razor | 启用 `Nullable` + `ImplicitUsings` |
| 前端 | 原生 HTML/CSS/JS | `index.html` 承载 Blazor，`liquid-glass.js` 毛玻璃，JS 桥接窗口控制/Toast |
| 依赖注入 | `Microsoft.Extensions.DependencyInjection` | 全部服务在 `Program.cs` 注册为 Singleton |
| 发布 | `dotnet publish -c Release -r win-x64 --self-contained -p:PublishSingleFile=true` | 单文件自包含 exe，免装运行时（`build-release.bat` / `build.ps1`） |
| 数据存储 | `%APPDATA%\.minecraft\` 为游戏目录，`%APPDATA%\.minecraft\oml\` 为启动器数据；设置在 `%LOCALAPPDATA%\OMLLauncher\settings.json` | 见 `PathService` |

**架构特点**：WinForms 只做窗口壳，所有 UI 逻辑在 Blazor Razor 组件里；C# 服务层负责全部业务（下载/启动/文件系统/网络），通过 DI 注入到组件；JS 仅做窗口拖拽、Toast、iframe 等少量桥接。

---

## 二、项目结构

```
ourmc-launcher-main/
├── Program.cs                 # 入口：建 Form、配 DI、挂 BlazorWebView
├── LauncherForm.cs            # WinForms 自定义窗口（无边框/圆角/动画/拖拽/缩放）
├── Main.razor                 # Blazor 根组件：路由 + 全局状态 + 页面装配
├── Models.cs                  # 核心领域模型（版本/账号/实例/模组/设置/日志/服务器）
├── Models/
│   ├── DownloadModels.cs      # 下载任务 DTO（状态机/进度/CDN源）
│   └── PerformanceSettings.cs # 性能预设模型
├── Components/
│   ├── Pages/                 # 20 个页面组件（首页/下载/版本/模组/实例/资源/服务器/设置/AI分析/崩溃报告/启动优化/启动参数/日志/账号管理/登录/浏览器/皮肤站...）
│   └── Shared/                # Sidebar / TopBar / RightSidebar / LaunchOptimizationPanel / BrowserChoiceDialog
├── Services/                  # 28 个业务服务（详见第四节）
├── wwwroot/                   # index.html(入口+JS桥) / css/app.css / liquid-glass.js / 字体 / 背景图 / 图标
├── ourmclauncher.csproj
├── build.bat / build-release.bat / build.ps1 / clean.bat
└── README.md
```


### 数据流

```
用户操作 → Razor 组件 → Service（DI 注入）→ 文件系统 / 网络 / 进程
              ↓ AppState.OnChange 事件
         StateHasChanged → UI 刷新
```

- `AppState` 是全局状态容器，属性 setter 触发 `OnChange`，`Main.razor` 订阅后刷新。
- 窗口操作走 JS→`[JSInvokable]`→`WindowService`（拖拽/最小化/关闭）。
- 游戏输出通过 `LaunchService` 重定向 → `LogService` 收集 → `LogViewerPage` 展示。

---

## 三、内核链路（下载 → 环境检查 → 启动）

这是启动器的核心链路，也是当前维护重点。

```
DownloadsPage → DownloadService.DownloadVersionAsync()
                  ├─ 版本清单 (version_manifest_v2.json)
                  ├─ 版本 JSON
                  ├─ client.jar（SHA1 校验）
                  ├─ 依赖库（libraries/）
                  ├─ 资源索引 + 资源对象（assets/objects/<2位hash>/<hash>，8路并发，增量跳过）
                  ├─ log4j 配置（assets/log_configs/）
                  └─ natives 提取（versions/<id>/natives/）
        ↓
HomePage → LaunchService.Launch()
             ├─ EnvironmentService.CheckEnvironment()（Java/内存/磁盘/文件/依赖）
             ├─ ResolveVersionChain()（解析 inheritsFrom 继承链，合并 mainClass/libraries/arguments）
             ├─ BuildLaunchArguments()
             │     ├─ 现代 arguments.game/jvm 解析 + ${var} 替换
             │     ├─ 旧版 minecraftArguments 回退
             │     ├─ OS 规则判断（windows/x86/版本正则，feature 规则排除）
             │     ├─ classpath（版本jar + 父版本jar + 库，Maven坐标回退）
             │     ├─ 离线 UUID（Java 兼容的 v3 MD5）
             │     └─ log4j 参数（文件存在时追加）
             └─ Process.Start() + 输出重定向
```

**内核关键能力已验证**：现代 `arguments` 解析、旧版 `minecraftArguments`、版本继承、离线 UUID、OS 规则、classpath、log4j、资源对象下载 —— 对 1.8.9 ~ 1.21.6 五个真实版本 JSON 跑通，0 未解析占位符。

---

## 四、各模块完善程度

图例：✅ 完成 ｜ 🟡 基本可用（有缺口）｜ 🟠 半成品 ｜ ⚪ 桩

### 4.1 内核 — 下载/启动/版本/环境/实例

| 模块 | 状态 | 说明 / 关键缺口 |
|---|---|---|
| **DownloadService** | ✅ | 完整下载流水线（client jar + 库 + 资源索引/对象 + log4j + natives）；所有官方文件按长度/SHA1 校验，使用 `.part` 临时文件后原子落盘，任一关键文件失败会终止安装。缺口：natives 仍为全量解压。 |
| **LaunchService** | ✅ | 现代参数解析 + 继承链 + 离线 UUID + OS 规则 + classpath + log4j。缺口：仅离线（token/uuid 走离线，正版接入待他人）。 |
| **VersionService** | ✅ | 扫描已装版本、类型推断（Forge/Fabric/Quilt/OptiFine）、JSON 持久化。缺口：`GameVersion` 元数据字段少。 |
| **InstanceService** | ✅ | 实例 CRUD + 导入导出(zip) + 备份。缺口：实例路径硬编码未走 PathService；导入不校验 zip。 |
| **EnvironmentService** | 🟡 | 检查框架完整（5 项 + 分级报告），能实际探测 Java 版本。**缺口较多**：`GetRequiredJavaVersion` 硬编码版本映射、不读版本 JSON 的 `javaVersion` 字段、**不覆盖 1.21+**（fallback 到 Java 8，错误）；`CalculateRequiredSpace` 硬编码 2048MB；语言文件路径错误（`assets/indexes/minecraft/lang`，实际在 `assets/objects`）；`CheckLibraries` 手推 Maven 路径、不用 `downloads.artifact.path`、不查 rules；`Is64BitJava` 靠路径含"64"不可靠。 |
| **VersionManagementService** | 🟠 | 框架在但完整性检查基本失效：`GetExpectedNativeFiles` 硬编码旧 DLL 名（对 LWJGL3 现代版全错）；`CleanupOldVersionsAsync` 空实现；`CheckLibraryRules` 用 `OSVersion.Platform`="Win32NT" 比对 "windows" → **永不匹配**；`CalculateVersionSize` 把整个 libraries 目录算进单版本。 |

### 4.2 内核 — 并行/重叠实现（需决策去留）

| 模块 | 状态 | 说明 |
|---|---|---|
| **EnhancedDownloadService** | 🟠 | 仍保留为待合并代码，但已从 DI 和主流程移除，当前安装统一使用经过校验的 `DownloadService`。后续应删除，或只将可靠的断点续传/CDN 能力合并进主线。 |
| **LaunchOptimizationService** | 🟠 | LaunchService 的劣化重复实现：硬编码 `uuid=0`、不解析 `arguments`、不处理继承/log4j、`CheckRules` 不查 OS。对模组版会生成错误参数。是否被调用存疑，若未调用即死代码。 |
| **LaunchPresetService** | 🟡 | 预设定义完整可用。**致命 bug**：`GetSystemMemoryMB` 用 `SystemPageSize/1024/1024` ≈ 0MB，内存判断全不可信；`BuildJvmArgs` 有格式错误参数；预设不持久化。 |

### 4.3 模组子系统（完善度最低，建议重构）

四个服务功能高度重叠且各有根本性 bug，是全项目质量最差的区域。

| 模块 | 状态 | 关键缺口 |
|---|---|---|
| **ModService** | 🟡 | `GetModsDir` 忽略 versionName → **无版本隔离**（所有版本共享 mods/）；Forge `authorList`（数组）用 `GetString()` 读取会抛异常被吞；禁用机制非标准。 |
| **ModManagementService** | 🟠 | `ParseMcmodInfo` **把 JSON 当属性文件逐行 Split 解析** → 对 mcmod.info 完全失效；Fabric `authors`/`contact` 当字符串读会抛异常；`CheckModCompatibilityAsync` 恒返回 true。与 ModService 启停机制不同（`.disabled` 后缀 vs 移目录），共存混乱。 |
| **ModRepositoryService** | 🟠 | Modrinth search 用 **POST**（应为 GET，facets 格式也错）→ 大概率失败；`GetModrinthDetails` 读 project 不返回的 `members` 字段；CurseForge 是桩；**无模组下载方法**；`DetectConflicts` 仅文件名去重。 |
| **ModDependencyService** | 🟠 | `ParseFabricDependencies` 期望 `depends` 是**数组**（实际是对象）→ 标准 Fabric 模组依赖完全解析不出；Forge `mods.toml` 是桩；mcmod.info 同样当属性文件解析；注入了 `DownloadService` 却从不调用（"自动下载缺失依赖"未实现）。 |

### 4.4 账号 / 认证 / 皮肤

| 模块 | 状态 | 说明 |
|---|---|---|
| **AccountManagerService** | ✅ | 多账号 CRUD + 默认迁移 + JSON 持久化。缺口：无并发锁；密码/token 明文存储。 |
| **MicrosoftAuthService** | 🟡 | 设备码、XBL、XSTS、Minecraft Token、Bearer Profile、刷新换链、账户保存与正版启动参数已闭环。剩余阻断项仅为团队替换正式 `ClientId` 并完成真实账号联调。 |
| **SkinService** | 🟡 | 登录/用户信息/头像 URL 可用。缺口：`GetUserProfileAsync` 拿到响应体后不解析、**恒返回 null**；`GetUserInfoFromSessionAsync` 正则抠 HTML 脆弱；`_authToken` 只赋字符串"session"未用于鉴权；Logout 不通知服务端。 |

### 4.5 基础设施

| 模块 | 状态 | 说明 |
|---|---|---|
| **SettingsService** | ✅ | 设置项 Get/Set 配对 + 即时持久化；API Key 使用 Windows DPAPI CurrentUser 加密，旧明文配置会自动迁移。缺口：`SaveSettings` 失败 throw（其它服务吞掉写日志），行为不一致。 |
| **PathService** | ✅ | 路径统一管理，构造时建目录。缺口：`GameDir` 依赖设置项，需确认 GameDirectory 默认非空串。 |
| **AppState** | ✅ | 全局状态容器，setter 触发刷新。缺口：纯内存（靠 SettingsService 落盘），与 SettingsService 字段重复需手动同步。 |
| **LogService** | ✅ | 日志收集/分级/过滤/导出/崩溃检测，线程安全。缺口：仅内存无轮转，长跑内存涨；需手动导出。 |
| **ThemeService** | ✅ | 深/浅/自动主题，读注册表判系统主题。缺口：主题不持久化，重启回默认 Dark；异常时默认浅色会跳变。 |
| **ServerService** | ✅ | 服务器列表 CRUD + 持久化。缺口：无 Ping/MOTD 探测。 |
| **WindowService** | 🟡 | 最小化/关闭/拖动，线程安全。缺口：缺最大化/还原/置顶/尺寸读取；`Close` catch 吞所有异常。 |
| **NotificationService** | 🟡 | Toast 四类通知。缺口：冗余的 DotNetRef 从未用；通知列表只写不读；JS 失败静默吞。 |

### 4.6 辅助服务

| 模块 | 状态 | 关键缺口 |
|---|---|---|
| **AIService** | 🟡 | 端到端可跑。**Anthropic 解析 bug**（在 content 数组上取 `text`，应取 `content[0].text`）→ 该渠道必失败；**DeepSeek 检测是死代码**（`sk-` 前缀已被 OpenAI 分支截断）；`Custom` provider 抛 NotSupportedException；模型名硬编码且过时。 |
| **PerformanceService** | 🟡 | 预设/持久化/内存检测(P/Invoke)/生成 options.txt 都在。`GenerateOptionsFile` 中 `chatVis:` 缺值；FastRender/FastMath 等多项设置定义了却未写入 options.txt（且部分是 OptiFine/Sodium 参数，非原版键）；新旧版键名混用。 |
| **PythonAutomationService** | 🟠 | **实际不可用**：`CreateDefaultScripts` 空桩（脚本不存在）→ 自动检测 Java/清缓存必失败；`DetectPythonAsync`/`ExecuteScriptAsync` 同时 `UseShellExecute=true` 且重定向输出 → .NET 互斥抛 `InvalidOperationException`。建议优先修复或明确下线。 |

### 4.7 数据模型

| 模块 | 状态 | 说明 |
|---|---|---|
| **Models.cs** | ✅ | 14 个类/枚举，字段完整有文档注释。缺口：`LoaderType` 缺 Quilt/OptiFine（与 `VersionType` 口径不一致）；`ModInfo` 缺 ModId/Dependencies 等依赖分析字段。 |
| **Models/DownloadModels.cs** | ✅ | 下载 DTO，状态机完整，已为断点续传/CDN 预留字段。 |
| **Models/PerformanceSettings.cs** | ✅ | 4 档预设配置合理。缺口：部分字段 PerformanceService 未消费。 |

### 4.8 UI 页面

| 页面 | 状态 | 未接通点 |
|---|---|---|
| AIAnalysisPage | ✅ | — 真实调 AIService，结果完整 |
| ModDependencyPage | ✅ | — 真实调 ModDependencyService |
| InstancesPage | ✅ | `SelectInstance` 空桩（minor） |
| ResourcesPage | ✅ | — |
| HomePage / DownloadsPage / SettingsPage / VersionsPage / ServersPage / LogViewerPage | ✅ | — 委托父组件/服务 |
| TopBar / Sidebar / RightSidebar / LaunchOptimizationPanel / BrowserChoiceDialog | ✅ | — |
| CrashReportPage | 🟡 | `CopyReportContent` 未写剪贴板（只弹"请手动复制"）；`AnalyzeWithAI` 调 AI 不展示结果；`RefreshReports` 有假延迟 |
| LaunchOptimizationPage | 🟡 | `SystemMemory` 硬编码 8192；`ApplyOptimizations`/`TestOptimizations` 仅弹通知无逻辑；提升百分比全硬编码；**LaunchOptimizationService 全程未调用** |
| LaunchParamsPage | 🟡 | 游戏参数/工作目录/环境变量的保存被注释掉；`BuildGameArguments` 从未被调用；部分开关不持久化 |
| ModsPage | 🟡 | `DownloadMod` 不下载只开浏览器；已安装列表重复渲染一遍 |
| AccountManagementPage | 🟡 | 微软/皮肤站登录为桩（微软属他人）；设备码流程开了头未实现（未使用字段） |
| AccountPage | 🟡 | `EditAccount` 空桩 |
| LoginPage | 🟡 | `HandleRegister` 桩 |
| BrowserPage | 🟠 | 前进/后退空实现；`Navigate` 假延迟；JS 回调空函数；历史状态恒 false |
| SkinSitePage | 🟠 | 无交互；`ShowError` 硬编码 false；JS 回调仅 console.log |

**UI 总体**：主体框架已搭、核心交互已接通，约 10/24 页面有未接通功能，集中在三类——"测试/应用/复制"按钮只弹通知（启动优化/启动参数/崩溃复制）、外部依赖功能为桩（微软登录/注册/浏览器导航/皮肤站）、表单字段能编辑但保存被注释（启动参数）。

---

## 五、完善度总览

| 状态 | 数量 | 代表模块 |
|---|---|---|
| ✅ 完成 | ~18 | DownloadService、LaunchService、VersionService、InstanceService、SettingsService、PathService、LogService、ThemeService、ServerService、AppState、AccountManagerService、3 个模型、多数 UI 页面 |
| 🟡 基本可用 | ~12 | EnvironmentService、ResourceService、ModService、LaunchPresetService、SkinService、AIService、PerformanceService、WindowService、NotificationService、MicrosoftAuthService、若干 UI 页面 |
| 🟠 半成品 | ~7 | VersionManagementService、ModManagementService、ModRepositoryService、ModDependencyService、EnhancedDownloadService、LaunchOptimizationService、PythonAutomationService、BrowserPage、SkinSitePage |

**一句话**：核心下载-启动链路完整可跑；外围（模组子系统、并行重复服务、部分辅助服务）存在系统性质量问题，多处"框架完整但解析逻辑根本错误"。

---

## 六、已知关键缺陷清单（按影响排序）

**P0 — 影响启动成功**
1. `EnvironmentService.GetRequiredJavaVersion` 不读版本 JSON 的 `javaVersion`、不覆盖 1.21+ → 现代版本可能被误判 Java 版本而拦截启动。
2. `EnvironmentService.CheckLibraries` 不用 `downloads.artifact.path`、不查 rules → 误报库缺失，可能阻止启动。

**P1 — 功能失效**
3. `ModManagementService.ParseMcmodInfo` / `ModDependencyService` 把 JSON 当属性文件解析 → 模组元数据/依赖解析完全失效。
4. `ModRepositoryService.SearchModrinthMods` 用 POST（应为 GET）→ 在线搜模组大概率失败。
5. `PythonAutomationService` 脚本不存在 + 进程配置互斥 → 整个服务不可用。
6. `AIService` Anthropic 解析 bug + DeepSeek 死代码 → 两渠道失效。
7. `LaunchPresetService.GetSystemMemoryMB` ≈ 0 → 预设推荐不可信。

**P2 — 数据/体验**
8. `SkinService.GetUserProfileAsync` 恒返回 null。
9. `VersionManagementService` 完整性检查多处失效（DLL 名、规则匹配、大小计算）。
10. `PerformanceService.GenerateOptionsFile` `chatVis` 缺值 + 多项设置未写入。
11. `EnhancedDownloadService` 漏资源对象、进度恒 0（未被主流程使用）。
12. UI 多处"按钮只弹通知"（启动优化/启动参数/崩溃复制）。

**共性风险**：敏感信息（密码/ApiKey/Token）明文落盘；多服务无并发锁写同一文件；异常被 `catch{}` 静默吞；服务职责重叠（Download×2、Launch×2、Mod×4）。

---

## 七、下一步计划

> 维护分工：**内核（P0/P1-内核）由本文档作者负责**；正版登录（P2-认证）由他人负责。

### P0 — 内核加固（优先，保证"装好就能启动"）
- [ ] **EnvironmentService 重写 Java 版本判断**：从版本 JSON 读 `javaVersion.component`/`requirements`，去掉硬编码映射，覆盖 1.21+。
- [ ] **EnvironmentService.CheckLibraries**：改用 `downloads.artifact.path` + rules 判断，复用 LaunchService 的 `CheckRules`/`LibraryNameToPath`。
- [ ] **EnvironmentService 空间计算**：按版本 JSON 实际文件大小累计，去掉硬编码 2048。
- [ ] **EnvironmentService 修语言文件路径**与 `Is64BitJava` 探测（改用 `java -version` 输出判断）。
- [ ] **natives 提取过滤**：只解压 `.dll`（含 `LWJGL` 平台库），跳过 META-INF/非本平台文件。
- [x] **库下载哈希校验**：client、库、资源、log4j、native 均按官方长度/SHA1 校验并原子落盘。

### P1 — 内核去重与完整性
- [x] **隔离 EnhancedDownloadService**：已从 DI/主流程移除；后续只合并经过测试的断点续传/CDN 能力。
- [ ] **决策 LaunchOptimizationService 去留**：与 LaunchService 重叠，确认是否被调用；未调用则删除，避免误用劣化实现。
- [ ] **VersionManagementService 完整性检查修复**：native 文件名从版本 JSON 提取、修 `CheckLibraryRules` 的 OS 匹配、实现 `CleanupOldVersionsAsync`、修大小计算。
- [ ] **LaunchPresetService 修 `GetSystemMemoryMB`**（用 P/Invoke `GlobalMemoryStatusEx`，PerformanceService 已有可复用）。

### P1 — 模组子系统重构（系统性问题，建议统一处理）
- [ ] 统一模组元数据解析器：正确解析 `mcmod.info`(JSON)、`fabric.mod.json`(对象/数组字段)、`mods.toml`，消除"JSON 当属性文件解析"。
- [ ] 统一模组启停机制（定一种：`.disabled` 后缀 or 移目录），消除 ModService/ModManagementService 冲突。
- [ ] `ModService.GetModsDir` 支持版本隔离（按实例/版本分 mods 目录）。
- [ ] `ModRepositoryService` 修 Modrinth search 为 GET + 正确 facets；加模组文件下载方法。
- [ ] `ModDependencyService` 修 `depends` 对象解析 + 接通"自动下载缺失依赖"。

### P2 — 辅助服务修复
- [ ] `PythonAutomationService`：补默认脚本 + 修 `UseShellExecute`/重定向互斥；或明确下线。
- [ ] `AIService`：修 Anthropic 解析（`content[0].text`）、修 DeepSeek 检测死代码、模型名可配置。
- [ ] `PerformanceService`：补 `chatVis` 值、清理无效键、区分原版/OptiFine/Sodium 参数。
- [ ] `SkinService.GetUserProfileAsync`：补解析逻辑。

### P2 — 正版登录（他人负责）
- [ ] `MicrosoftAuthService`：认证与刷新换链已修复；仅待团队替换正式 ClientId 并进行真实账号联调。
- [ ] 将账号 token 接入 `LaunchService.Launch()`（替换离线 `auth_access_token`/`auth_uuid`/`auth_xuid`/`clientid`，变量表已就绪）。

### P3 — UI 半成品收尾
- [ ] `LaunchParamsPage`：接通游戏参数/工作目录/环境变量保存，接 `BuildGameArguments`。
- [ ] `LaunchOptimizationPage`：读真实内存、`Apply/Test` 接真实逻辑、调 LaunchOptimizationService。
- [ ] `CrashReportPage`：`CopyReportContent` 写剪贴板、`AnalyzeWithAI` 展示结果。
- [ ] `BrowserPage`/`SkinSitePage`：明确 iframe 能力边界或换实现。

### P3 — 工程化加固
- [ ] 消除重叠服务职责（Download/Launch/Mod 各保留一套主线）。
- [ ] 敏感信息加密存储（DPAPI），配置文件加并发锁。
- [ ] `LogService` 落盘 + 轮转上限。
- [ ] 清理 23 个编译警告（fire-and-forget、空引用解引用、死字段）。

---

## 八、附：内核已验证清单（本轮完成）

- ✅ 修复编译错误（`RightSidebar.razor` 的 EventCallback 误用，原项目无法生成 exe）
- ✅ `DownloadService` 补全资源对象下载 + log4j 配置下载 + 进度条重平衡
- ✅ `LaunchService` 重写：现代 `arguments` 解析 + `${var}` 替换 + 旧版回退 + OS 规则 + 版本继承 + 离线 UUID + classpath + log4j 参数
- ✅ 对 1.8.9 / 1.16.5 / 1.18.2 / 1.20.4 / 1.21.6 真实版本 JSON 验证启动参数（0 未解析占位符）
- ✅ 资源下载数据契约对线上 Mojang API 验证通过
- ✅ 全程 `dotnet build` 0 错误

**待真机验证**：下载某版本 → 启动 → 进游戏（贴图/声音正常、无报错）。本环境无法跑 GUI + 200MB 下载，需在开发机执行。
